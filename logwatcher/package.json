{
  "name": "watcherjs",
  "version": "0.0.1",
  "description": "A simple utilty that watches a folder for new files, and performs an action when one is created.",
  "main": "rlmonitor.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jxmot",
  "license": "MIT",
  "dependencies": {
    "mysql": "^2.18.1",
    "simple-text-log": "0.0.1"
  }
}
